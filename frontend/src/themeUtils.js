// themeUtils.js
export const bgClass = (theme) => theme === 'dark' ? 'bg-dark' : 'bg-light';
export const textClass = (theme) => theme === 'dark' ? 'text-light' : 'text-dark';
export const tableVariant = (theme) => theme === 'dark' ? 'dark' : 'light';
